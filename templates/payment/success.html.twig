{% extends 'base_front.html.twig' %}

{% block title %}Payment Successful - Edu9raya{% endblock %}

{% block body %}
    <!-- Page Title Section -->
    <section class="page-title" style="background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url('{{ asset('assets/img/hero-bg.jpg') }}') center/cover; padding: 100px 0;">
        <div class="container" data-aos="fade-up">
            <div class="row justify-content-center">
                <div class="col-lg-8 text-center text-white">
                    <h1>Payment Successful!</h1>
                    <p>Thank you for your payment</p>
                </div>
            </div>
        </div>
    </section>

    <div class="container my-5">
        <div class="row justify-content-center">
            <div class="col-md-8 text-center">
                <div class="card shadow-sm">
                    <div class="card-body p-5">
                        <div class="mb-4">
                            <i class="fas fa-check-circle text-success" style="font-size: 4rem;"></i>
                        </div>
                        <h2 class="mb-4">Payment Successful</h2>
                        <p class="lead mb-4">Your payment has been processed successfully. Thank you for your purchase!</p>
                        <a href="{{ path('app_front_events') }}" class="btn btn-primary">
                            <i class="fas fa-arrow-left me-2"></i> Return to Events
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
